.DS_Store
.vercel
node_modules
*.log
# secrets — the repo is public
.env
.env.*
!.env.example
# large native/source exports — keep out of git
*_BM_*.csv
# The demo's block model is generated, not committed: 25 MB that
# tools/make_demo_model.py reproduces exactly from its seed.
data/demo_model_source.csv

# local hydration fixtures — generated, never committed
.fixtures/

# The Williams deploy copy. THIS REPO IS PUBLIC and williams/ holds a client's
# derived drill assays and geophysics — it is built here only so Vercel serves
# it as a subpath. Source and backup live in the PRIVATE shayne-wq/bedrock-williams.
williams/
